{
  "name": "purpose-piece",
  "version": "1.0.0",
  "description": "Purpose Piece conversational assessment",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
